//import { tvly } from '@/lib/tavilyClient';

export async function plannerWorker(searchReq: string) {
    console.log(`[plannerWorker]: ${searchReq}`)
    // const response = await tvly.search("Who is Leo Messi?");
    // return response;
}